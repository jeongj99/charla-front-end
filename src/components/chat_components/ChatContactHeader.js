import './ChatContactHeader.css'

export default function ChatContactHeader(props) {
  const { chatListState } = props;
  console.log('Hello from all the info inside your chat contact header', chatListState)

  return (
    <div className="chat-contact-header">
    
  </div>
  );
}